#version 330 core

out vec4 fragColor;

varying vec3 v_worldPos;
varying vec3 v_normal;
varying vec2 v_texcoord;

uniform sampler2D u_albedoSampler;

void main()
{
	fragColor = texture(u_albedoSampler, v_texcoord);
}